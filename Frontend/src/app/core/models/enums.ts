export enum CpiInterpretation {
  UNDER_BUDGET = 'Bajo presupuesto',
  IN_BUDGET = 'En presupuesto',
  OVER_BUDGET = 'Sobre presupuesto'
}

export enum SpiInterpretation {
  AHEAD = 'Adelantado',
  ON_SCHEDULE = 'En cronograma',
  BEHIND = 'Atrasado'
}

export enum HealthStatus {
  HEALTHY = 'healthy',
  WARNING = 'warning',
  CRITICAL = 'critical'
}
